{
  "file": "queries/my_query.sql",
  "reviewed_at": "2026-05-25T16:12:06.779746",
  "issues_found": 5,
  "review": {
    "issues": [
      {
        "severity": "Critical",
        "category": "Correctness",
        "title": "Incorrect revenue calculation",
        "line": "SUM(t.amount) AS total_revenue",
        "problem": "The query does not filter transactions by status 'COMPLETED' for revenue calculation.",
        "fix": "SUM(CASE WHEN t.status = 'COMPLETED' THEN t.amount ELSE 0 END) AS total_revenue"
      },
      {
        "severity": "Critical",
        "category": "Correctness",
        "title": "Incorrect failure rate calculation",
        "line": "ROUND(100.0 * (SELECT COUNT(*) FROM fact_transactions f3 WHERE f3.merchant_id = m.merchant_id AND f3.status = 'FAILED') / NULLIF(COUNT(t.transaction_id), 0), 2) AS failure_rate_pct",
        "problem": "The failure rate calculation does not filter transactions by status 'COMPLETED' for the denominator.",
        "fix": "ROUND(100.0 * (SELECT COUNT(*) FROM fact_transactions f3 WHERE f3.merchant_id = m.merchant_id AND f3.status = 'FAILED') / NULLIF(COUNT(CASE WHEN t.status = 'COMPLETED' THEN t.transaction_id ELSE NULL END), 0), 2) AS failure_rate_pct"
      },
      {
        "severity": "Medium",
        "category": "Performance",
        "title": "Correlated subqueries",
        "line": "Subqueries in SELECT clause",
        "problem": "Correlated subqueries can be inefficient as they are executed for each row in the result set.",
        "fix": "Use JOINs instead of correlated subqueries."
      },
      {
        "severity": "Low",
        "category": "Readability",
        "title": "Lack of comments",
        "line": "Entire query",
        "problem": "The query lacks comments explaining the purpose and logic of each part.",
        "fix": "Add comments to explain the purpose and logic of each part of the query."
      },
      {
        "severity": "Low",
        "category": "Readability",
        "title": "Implicit join syntax",
        "line": "FROM fact_transactions t, dim_merchant m, dim_customer c",
        "problem": "Using implicit join syntax can make the query harder to read and understand.",
        "fix": "Use explicit JOIN syntax."
      }
    ],
    "corrected_sql": "-- Merchant Performance Dashboard Query\n-- Purpose: Show top merchants by revenue with customer and failure stats\n\nSELECT\n    m.merchant_name,\n    m.category,\n    m.city,\n    SUM(CASE WHEN t.status = 'COMPLETED' THEN t.amount ELSE 0 END) AS total_revenue,\n\n    COUNT(t.transaction_id) AS total_txns,\n\n    (SELECT COUNT(*)\n     FROM fact_transactions f2\n     WHERE f2.merchant_id = m.merchant_id\n       AND f2.status = 'FAILED') AS failed_cnt,\n\n    ROUND(100.0 * (SELECT COUNT(*)\n                    FROM fact_transactions f3\n                    WHERE f3.merchant_id = m.merchant_id\n                      AND f3.status = 'FAILED')\n            / NULLIF(COUNT(CASE WHEN t.status = 'COMPLETED' THEN t.transaction_id ELSE NULL END), 0), 2) AS failure_rate_pct,\n\n    c.customer_name,\n    c.email,\n\n    SUM(CASE WHEN t.status = 'COMPLETED' THEN t.amount ELSE 0 END) / COUNT(CASE WHEN t.status = 'COMPLETED' THEN t.transaction_id ELSE NULL END) AS avg_completed_txn_value\n\nFROM fact_transactions t\nJOIN dim_merchant m ON t.merchant_id = m.merchant_id\nJOIN dim_customer c ON t.customer_id = c.customer_id\n\nGROUP BY m.merchant_name, m.category, m.city, c.customer_name, c.email\n\nORDER BY total_revenue ASC;",
    "summary": "The query had critical correctness issues with revenue and failure rate calculations. It also had performance and readability issues with correlated subqueries and implicit joins."
  }
}